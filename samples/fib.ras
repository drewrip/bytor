fun fib(n: int32) -> int32
    if n == 0 then
        return 0;
    else if n == 1 then
        return 1;
    end

    return fib(n - 1) + fib(n - 2);
end

program calc_fib
    let index = 7;
    return fib(index);
end
