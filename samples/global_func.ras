let someLongIdent = 1233321;

function baz(x: int64, y: int64) -> int64
with (mut someLongIdent)
    someLongIdent = 3;

    let z = endfunc(someLongIdent, someLongIdent);
end

program test_program with (mut someLongIdent, anotherVar)
    let foo = (1 * 2) - 2;
    foo += 9986;
    baz(2, 3);
end

function endfunc(x: int64, y: int64) -> int64
with (mut someLongIdent)
    someLongIdent = 3;
end
