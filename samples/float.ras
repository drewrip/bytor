fun foo(x: float64) -> float64 begin
    return 2.0 * x;
end

program test_program begin
    let x = 5.0 / 2.0;
    let y = 0;
    if x > 2.4 then
        y = 1;
    else then
        y = 2;
    end
    return y;
end
